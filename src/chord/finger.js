export class Finger {
  /*
  *
  * start: string;
  * interval: [string, string];
  * node: ChordNode;
  *
  */

  constructor(start, interval, node) {
    this.start = start;
    this.interval = interval;
    this.node = node;
  }
}